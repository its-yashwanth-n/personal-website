import React from "react";
import { Section } from "@/styles/CommonStyles";
import { Typography } from "@mui/material";

const Projects = () => {
  return (
    <Section id="projects-section" name="projects">
      <Typography>Projects</Typography>
    </Section>
  );
};

export default Projects;
